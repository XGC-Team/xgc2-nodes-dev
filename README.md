# xgc2-nodes-dev

Open XGC orchestration nodes for marker intake, repository analysis, patching,
tests, and review.

Initial node: `xgc.dev.marker-normalize/v1` turns a product-side marker into a
stable, deterministic work item. Repository mutation remains a separate
effectful node with explicit repository grants.
